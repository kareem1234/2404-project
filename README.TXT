To run program execute commands:
				make
				./Assignment3

In our view a summary menu for teachers, if all applications are selected to be viewed and then the user chooses to save
these to file, they are stored in a text file named All Applications.txt. If a specific course is selected, then if the user
saves that data to file it will a text file with the name of it correspondin to the course currently selected.

HAVE FUN!!


